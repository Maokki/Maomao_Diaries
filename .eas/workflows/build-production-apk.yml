name: Build Production APK

on:
  push:
    branches: ['main']

jobs:
  build_android_apk:
    name: Build Android Production APK
    type: build
    params:
      platform: android
      profile: production